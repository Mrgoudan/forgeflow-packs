# machine-local pack configs (the real bsc/project.yaml IS tracked)
*/project.yaml
!bsc/project.yaml

# python
__pycache__/
*.pyc

# real secrets — NEVER commit
secrets.env

# runtime state (db, worktrees, archived outputs) — machine-local
*/.run/
